---
import { turso } from "src/turso";

const receptionID = Astro.url.searchParams.get("receptionID");

await turso.execute(`UPDATE PurchaseOrders SET statusID = 17 WHERE purchaseOrderID = ${receptionID}`);

return Astro.redirect('/reception')
---