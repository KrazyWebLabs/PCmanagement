---
import { turso } from "src/turso";

const pcID = Astro.url.searchParams.get("pcID");

await turso.execute(`
  UPDATE PC
  SET statusID = 4,
    userID = null
  WHERE id == ${pcID}
`)

return Astro.redirect('/pc')
---